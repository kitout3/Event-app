# Default / for a custom domain. GitHub Pages uses a portable relative base (./)
# in .github/workflows/deploy.yml, so a repository rename needs no env change.
VITE_APP_BASE_PATH=/
# Optional: override the public Firebase web configuration in config/firebase.public.json.
# Never put service-account credentials or private keys in VITE_ variables.
